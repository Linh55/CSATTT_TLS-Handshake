1. Cách cài đặt code:
   git clone https://github.com/<your-username>/<your-repo>.git
   cd <your-repo>
   pip install -r requirements.txt
   streamlit run app.py
   
2. Cách sử dụng:
   - Nhấn nút Start TLS Handshake
   - Nhập plaintext
   - Xem:
      Client / Server
      Shared Secret
      Session Key
      Ciphertext
   
